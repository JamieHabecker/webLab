article.g17
	div
		h3.formTitle Vehicle Information <span>(provide as much information as possible)</span>
		form(novalidate, name="form")
			.formField.year(year)

			.formField.make(make)

			.formField.model(model)

			.formField.vin(vin)

			.formField.plate(plate)

			.formField.prmColr(pcolor)

			.formField.secColr(scolor)

			.continue
				div(btn,act="Continue")
			portalcancel